<section class="layout">
	<% @assets.each do |asset| %>
		<div class="asset">
			<a href="/asset/<%= asset.id %>"><img src="<%= asset.image.thumb.url %>"></a>
			<p><%= asset.name %></p>
		</div>
	<% end %>
	
</section>